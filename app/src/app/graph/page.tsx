import { ComingSoon } from "@/components/layout/coming-soon";

export default function GraphPage() {
  return (
    <ComingSoon
      currentPath="/graph"
      heading="graph explorer"
      body="Full-screen network explorer lands in Plan 3."
      planName="PLAN 3 · EXPLORER"
    />
  );
}
