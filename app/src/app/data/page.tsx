import { ComingSoon } from "@/components/layout/coming-soon";

export default function DataPage() {
  return (
    <ComingSoon
      currentPath="/data"
      heading="data explorer"
      body="Predefined queries + CSV export land in Plan 3."
      planName="PLAN 3 · DATA"
    />
  );
}
